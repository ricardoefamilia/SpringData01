<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
xmlns:context="http://www.springframework.org/schema/context"
xmlns:jpa="http://www.springframework.org/schema/data/jpa"
xsi:schemaLocation="
	http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.0.xsd
	http://www.springframework.org/schema/data/jpa
	http://www.springframework.org/schema/data/jpa/spring-jpa.xsd"
	default-lazy-init="true">
	
	<!-- identifica as anotações do projeto -->
	<context:annotation-config />
	
	<!-- component-scan lê o model e identifica as classes persistentes e cria as tabelas -->
	<context:component-scan base-package="com.SpringData01.model" />
	
	<!-- Define as configurações de conexão com BD -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.postgresql.Driver"/>
		<property name="url" value="jdbc:postgresql://localhost:5432/dbspring01"/>
		<property name="username" value="postgres"/>
		<property name="password" value="admin"/>
	</bean>
	
	<!-- Define as configurações do JPA -->
	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean" scope="singleton">
		<property name="dataSource" ref="dataSource"/> <!-- lê as configurações de conexão com BD acima -->
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="database" value="POSTGRESQL"/>
				<property name="generateDdl" value="true"/> <!-- Gera as tabelas no banco -->
			</bean>
		</property>
		<property name="persistenceUnitName" value="SpringData01"/>
	</bean>
	
	<!-- Ativa o controle transacional -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager" scope="singleton">
		<property name="entityManagerFactory" ref="entityManagerFactory"/> <!-- lê as configurações do JPA acima -->
	</bean>
	
	<!-- repositories aponta para o dao, ativa o repositório de persistência -->
	<jpa:repositories base-package="com.SpringData01.dao" />
</beans>
